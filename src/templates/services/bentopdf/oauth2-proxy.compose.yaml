# BentoPDF + OAuth2 Proxy stack
# - oauth2-proxy performs authentication
# - bentopdf serves the app behind the proxy

services:
  oauth2-proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:latest
    container_name: bentopdf-oidc
    security_opt:
      - no-new-privileges:true
    command: --config /oauth2-proxy.cfg
    environment:
      # Common runtime identity
      TZ: ${TZ}
      PUID: ${PUID}
      PGID: ${PGID}
    ports:
      - ${BENTOPDF_OAUTH2_PROXY_PORT:-7860}:7860
    volumes:
      - ${BENTOPDF_OAUTH2_PROXY_CONFIG_FILE:-./oauth2-proxy.cfg}:/oauth2-proxy.cfg:ro
      - ${BENTOPDF_OAUTH2_PROXY_AUTHENTICATED_EMAILS_FILE:-./oauth2-proxy/authenticated_emails}:/authenticated_emails:ro
    networks:
      bentopdf-oauth2: {}
    depends_on:
      - bentopdf
    restart: on-failure:5

  bentopdf:
    image: ghcr.io/alam00000/bentopdf-simple:latest
    container_name: bentopdf
    security_opt:
      - no-new-privileges:true
    environment:
      # Common runtime identity
      TZ: ${TZ}
      PUID: ${PUID}
      PGID: ${PGID}
    networks:
      bentopdf-oauth2: {}
    restart: on-failure:5
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  bentopdf-oauth2: {}
